const authEventPattern = {
  AUTH_LOGIN: 'auth.login',
  VERIFY_TOKEN: 'auth.verify.token',
  DECODED_TOKEN: 'auth.decoded.token',
}

export const { AUTH_LOGIN, VERIFY_TOKEN, DECODED_TOKEN } = authEventPattern
